apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: core-platform
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:angelpixel-core/chatbot-ai-rag-langchain.git
    targetRevision: main
    path: infra/delivery/core-platform/overlays/nonprod
  destination:
    server: https://kubernetes.default.svc
    namespace: platform
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
    syncOptions:
      - CreateNamespace=true
