# Local Infra

Este directorio contiene la infraestructura local del proyecto.

## Archivos

- `compose.yaml` - definición principal del stack local.
- `containers/` - contenedores y entrypoints locales.

## Puertos externos

- `server` - `10001:8000`
- `web` - `10002:3000`
- `db` - `10003:5432`
