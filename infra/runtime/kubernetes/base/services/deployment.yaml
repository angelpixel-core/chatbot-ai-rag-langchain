apiVersion: apps/v1
kind: Deployment
metadata:
  name: services
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: services
  template:
    metadata:
      labels:
        app.kubernetes.io/name: services
    spec:
      containers:
        - name: services
          image: coffee-chatbot-services:local
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - /app/infra/runtime/containers/services/entrypoint.sh
          args:
            - python
            - apps/services/chatbot/manage.py
            - runserver
            - 0.0.0.0:8000
          envFrom:
            - configMapRef:
                name: runtime-config
            - secretRef:
                name: runtime-secrets
          ports:
            - name: http
              containerPort: 8000
          readinessProbe:
            httpGet:
              path: /admin/
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /admin/
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
