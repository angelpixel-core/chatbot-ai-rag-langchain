apiVersion: v1
kind: Service
metadata:
  name: services
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: services
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30080
